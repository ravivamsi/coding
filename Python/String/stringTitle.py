# Define a String

str = "pyThON"

# Convert String in Title Case and assign to a variable strtitle

strtitle = str.title();

# Print both the original and the converted fields

print(str+ " is converted to the Lower Case as "+ strtitle);
